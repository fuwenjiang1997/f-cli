# 文档

项目介绍

基于TypeScript的命令行工具，用于创建Vue，React项目。

## 安装

克隆项目到本地，进入项目目录，执行以下命令安装依赖：

```sh
pnpm install
cd packages/cli
pnpm link
```

## 使用

```sh
f-cli create my-project #创建项目
f-cli info #查看cli信息
```
